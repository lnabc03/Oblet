# Oblet 打磨记录（2026-07-31 批次 1–3 首轮）

> 本轮执行《Oblet-打磨清单.md》批次 1 全部、批次 2 首轮、批次 3 前七项。
> 每项均通过 `npm run build` 类型检查；改序列化相关处均跑过两个回归脚本（8 项全 PASS）。
> 提交序列：`8e550b5`（批次 1）→ `cef801d`（2.2）→ `8dbd813`（3.1/3.2）→ `c0ea2ff`（3.3）→ `8dfb650`（3.4）→ `d1f3d26`（3.5）→ `d5c5123`（3.6）→ `47cb0aa`（3.10）。

---

## 批次 1：已知 bug 速修（全部完成）

| 项 | 根因与修法 |
|---|---|
| 1.1 标题顺序 | `lib.rs` + `commands.rs` 两处格式串 → `Oblet - 文件名` |
| 1.2 属性栏光标不可见 | virtual-cursor 给 `.ProseMirror` 设 `caret-color: transparent`，**该属性可继承**，属性栏 input 被连坐。`.ob-frontmatter input` 作用域恢复 `caret-color` |
| 1.3 起始页设置打不开 | `boot()` 先挂设置浮层进 #app，无文件分支随后 `app.innerHTML=` 把浮层抹掉；按钮幸存但切换的是脱离文档的节点。改 createElement + appendChild |
| 1.4 斜杠菜单删减 | 按批示删 Quote/Divider/H4-H6/Image/Math（BlockEdit 配置项置 `null`，官方机制；语法本身不受影响） |
| 1.5 logo 不醒目 | `scripts/brighten-icon.mjs`：晶体中调 gamma 0.65 + 饱和 1.3，背景不动。图标全套重生成 + exe 重嵌 + zip 重封（2.7MB） |
| 1.6 高亮复合 bug（P0） | 两根因：① PM 把装饰 span 包在 strong/em **内侧**，`.ob-highlight` 钉死的 `color` 压掉 anp-bold-red/italic-green——删 color 声明；② 旧实现遇行内代码断 run，`==`代码`==` 无法配对——重写为虚拟串方案（代码节点占一格，代码内 == 不配对、跨代码允许配对） |

## 批次 2

- **2.1 dev 卡死悬案**：未动代码，**等你在 release 版复测定性**（zip 已含批次 1 修复）。
- **2.2 屎山治理首轮**：frontmatter.ts 8 处兜底全部已有注释固化、无可删死代码；plugins.ts 两插件骨架重复 → 提取 `makeDecoratorPlugin` 工厂。结论：自有代码 ~900 行，魔改比预想收敛。

## 批次 3（前七项）

| 项 | 内容 |
|---|---|
| 3.1 底部留白 | 核对时发现前序已实现（`padding: 24px 32px 40vh`） |
| 3.2 起始页 | logo（提亮版打包进前端 + mauve 光晕）+ 操作提示 + 署名 **弋鹓 \| lnabc03**，全走主题变量 |
| 3.3 检索（P0） | `src/editor/search.ts`：Ctrl+F 浮条、装饰器高亮（文档不动）、Enter/Shift+Enter 循环跳转滚动居中、命中计数、打开预填选区。**编辑器内 Enter 不劫持**（对齐 Ob）。替换功能待你实测检索手感后再加 |
| 3.4 通知系统 | `src/notify.ts` 三级 toast（4/6/9s 自动消失、点击即关、滑入滑出）。**保存失败原来只 console.error，现在用户可见** |
| 3.5 快捷操作栏 | `src/editor/toolbar.ts`：bubble toolbar 追加 ==高亮== 开关（文本级，序列化无损）+ callout 包裹（块级，光标态包当前块） |
| 3.6 右键菜单 | `src/editor/contextmenu.ts`：复制/剪切/粘贴/全选 + 高亮/callout（与工具栏共用命令），替换浏览器系统菜单 |
| 3.10 代码块换行 | 设置面板新增"编辑器"节：`code_block_wrap` 复选框，`body.ob-code-wrap` 即时切换。顺带发现主题的 `anp-codeblock-edit-nowrap` 是死类 |

## 未完成 / 待续

- 批次 2.1（等你 release 复测）、3.3 替换、3.7 图片块实测、3.8 callout 样式、3.9 浮层主题化
- 批次 4 设置体系（4.1 毛玻璃、4.2 设置页、4.3 默认值实体化、4.4 快捷键、4.5 当前行高亮决策）
- 批次 5 性能基线、批次 6 发布检查

---

## 测试提示（按改动风险排序，重点在前）

1. **高亮复合渲染（1.6，改动最大）**：`==**粗**==`（粗体红应透出）、`**==粗==**`、`==*斜*==`、`` ==`代码`== ``（代码应被包进高亮）、`` `==` ``（代码内 == 应保持字面不高亮）。顺手 Ctrl+S 后看源文件无变化。
2. **检索（3.3）**：Ctrl+F 开浮条 → 输入看命中计数与全文档高亮；Enter/Shift+Enter 循环跳转且滚动居中；**浮条开着时在正文按 Enter 应照常换行**；Esc 关闭；选中一段文字再 Ctrl+F 应预填。
3. **工具栏/右键（3.5/3.6）**：选中文字 → bubble 工具栏尾部出现高亮笔与引号两按钮，点了看 `==` 与 `> [!note]` 序列化是否正确；右键菜单各条目（粘贴若失败会提示用 Ctrl+V，属预期降级）。
4. **起始页（1.3/3.2）**：空窗口应见 logo+署名；**Ctrl+, 设置能打开**（1.3 的修复点）；改字号起始页立即生效。
5. **设置（3.10）**：设置 → 编辑器 → 代码块自动换行，长行代码块切换看效果；重开窗口应保持。
6. **属性栏（1.2）**：单元格里应能看到闪烁光标。
7. **通知（3.4）**：外部改文件（有未保存内容时）弹 warn toast；可顺手验证断网/占用导致保存失败弹 error toast（不好模拟可跳过）。
8. **2.1 悬案复测**：用 release zip 版反复拖拽多个不同 md 进同一窗口，看是否还有卡死；没有就按 dev-only 结案。
