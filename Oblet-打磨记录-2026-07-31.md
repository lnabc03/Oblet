Oblet 打磨记录（2026-07-31 批次 1–3 首轮）

> 本轮执行《Oblet-打磨清单.md》批次 1 全部、批次 2 首轮、批次 3 前七项。
> 每项均通过 `npm run build` 类型检查；改序列化相关处均跑过两个回归脚本（8 项全 PASS）。
> 提交序列：`8e550b5`（批次 1）→ `cef801d`（2.2）→ `8dbd813`（3.1/3.2）→ `c0ea2ff`（3.3）→ `8dfb650`（3.4）→ `d1f3d26`（3.5）→ `d5c5123`（3.6）→ `47cb0aa`（3.10）。

---

## 批次 1：已知 bug 速修（全部完成）

| 项                | 根因与修法                                                                                                                                                                          | 反馈                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------- |
| 1.1 标题顺序         | `lib.rs` + `commands.rs` 两处格式串 → `Oblet - 文件名`                                                                                                                                 | 通过                                                                |
| 1.2 属性栏光标不可见     | virtual-cursor 给 `.ProseMirror` 设 `caret-color: transparent`，**该属性可继承**，属性栏 input 被连坐。`.ob-frontmatter input` 作用域恢复 `caret-color`                                              | 通过，但有一个小问题，第一列和第二列点击时的高亮框宽度设置不一样，第一列自适应，第二列全占满，应该统一为第二列的样式        |
| 1.3 起始页设置打不开     | `boot()` 先挂设置浮层进 #app，无文件分支随后 `app.innerHTML=` 把浮层抹掉；按钮幸存但切换的是脱离文档的节点。改 createElement + appendChild                                                                            | 按钮打得开，但目前快捷键尚未生效                                                  |
| 1.4 斜杠菜单删减       | 按批示删 Quote/Divider/H4-H6/Image/Math（BlockEdit 配置项置 `null`，官方机制；语法本身不受影响）                                                                                                       | 通过，但新增代码块选择语言时存在无选项弹出bug，考虑取消代码块选项，用户可填入任意语言（包括自定义的），在预设列表中就给配置高亮 |
| 1.5 logo 不醒目     | `scripts/brighten-icon.mjs`：晶体中调 gamma 0.65 + 饱和 1.3，背景不动。图标全套重生成 + exe 重嵌 + zip 重封（2.7MB）                                                                                     | 基本通过，后续有更好的选择我会自行调整                                               |
| 1.6 高亮复合 bug（P0） | 两根因：① PM 把装饰 span 包在 strong/em *内侧*，`.ob-highlight` 钉死的 `color` 压掉 anp-bold-red/italic-green——删 color 声明；② 旧实现遇行内代码断 run，`==`代码`==` 无法配对——重写为虚拟串方案（代码节点占一格，代码内 == 不配对、跨代码允许配对） | 通过，这里Obsidian也有bug，平常用的人不多，暂不再修                                   |

## 批次 2

- **2.1 dev 卡死悬案**：未动代码，**等你在 release 版复测定性**（zip 已含批次 1 修复）。
- **2.2 屎山治理首轮**：frontmatter.ts 8 处兜底全部已有注释固化、无可删死代码；plugins.ts 两插件骨架重复 → 提取 `makeDecoratorPlugin` 工厂。结论：自有代码 \~900 行，魔改比预想收敛。

## 批次 3（前七项）

| 项          | 内容                                                                                                                            | 反馈                                                                                                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------- |
| 3.1 底部留白   | 核对时发现前序已实现（`padding: 24px 32px 40vh`）                                                                                         | 通过                                                                                                                |
| 3.2 起始页    | logo（提亮版打包进前端 + mauve 光晕）+ 操作提示 + 署名 **弋鹓 \| lnabc03**，全走主题变量                                                                 | 通过，挺好看的，后续删掉快捷键提示，提供设置选项可隐藏作者信息                                                                                   |
| 3.3 检索（P0） | `src/editor/search.ts`：Ctrl+F 浮条、装饰器高亮（文档不动）、Enter/Shift+Enter 循环跳转滚动居中、命中计数、打开预填选区。**编辑器内 Enter 不劫持**（对齐 Ob）。替换功能待你实测检索手感后再加 | 通过，但有一个问题，用户按两下Ctrl+F，第一下正常打开软件的检索，第二次弹出浏览器默认检索（和聚焦有关）。需禁用后者。                                                     |
| 3.4 通知系统   | `src/notify.ts` 三级 toast（4/6/9s 自动消失、点击即关、滑入滑出）。**保存失败原来只 console.error，现在用户可见**                                              | 当前未触发过                                                                                                            |
| 3.5 快捷操作栏  | `src/editor/toolbar.ts`：bubble toolbar 追加 ==高亮== 开关（文本级，序列化无损）+ callout 包裹（块级，光标态包当前块）                                        | 高亮通过，但右键提示写的是“==高亮”，需要删除前面的符号。callout基本通过，但目前似乎只能添加Note样式，后续新增几种（目前用户无法主动创建其他样式的callout），另外用户在原处按两下这个按钮应该可以回退到原样。 |
| 3.6 右键菜单   | `src/editor/contextmenu.ts`：复制/剪切/粘贴/全选 + 高亮/callout（与工具栏共用命令），替换浏览器系统菜单                                                      | 通过，问题同上                                                                                                           |
| 3.10 代码块换行 | 设置面板新增"编辑器"节：`code_block_wrap` 复选框，`body.ob-code-wrap` 即时切换。顺带发现主题的 `anp-codeblock-edit-nowrap` 是死类                           | Search language 功能坏了，目前无选项，代码自动换行似乎也未生效                                                                           |

## 未完成 / 待续

- 3.7 图片块实测、3.8 callout 观感（均需你实测/目测）
- 批次 4 设置体系（4.1 毛玻璃、4.2 设置页、4.4 快捷键、4.5 当前行高亮决策）——按你批示"最后一起做"
- 批次 5 性能基线（需运行实测）、批次 6 发布检查
- 2.1 已按复测结论搁置（小概率触发、原因不明，移入清单 §八）

---

## 二轮反馈修订（2026-07-31，按反馈列逐条处理）

| 反馈                      | 根因与修法                                                                                                                                                                          | 反馈                                                                                               |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------------- |
| 1.2 属性栏两列高亮框宽度不一        | 键名列 input 有 `width:auto` + `field-sizing:content` 特例 → 删除，两列统一填满单元格                                                                                                            | 出现了新问题，现在第一列内容不可见，列宽被压到了最小。                                                                      |
| 1.3 / 提示4 Ctrl+, 不生效    | 快捷键监听改 **capture 阶段 + `e.code === "Comma"`**：中文输入法下 `e.key` 可能是全角"，"；CM/PM 内部编辑器可能 stopPropagation 阻断冒泡监听                                                                      | 仍不生效，但改为Ctrl+/或其他合适的快捷键就没问题，将Ctrl+/作为默认，不用调整了                                                    |
| 1.4/3.10 代码块语言选择空白      | **Crepe 不传 `languages` 时以空数组覆盖组件默认配置** → 显式传入 `@codemirror/language-data` 全量预设（升为直接依赖）；语言支持按需懒加载（构建产物已自动按语言分包）                                                                 | 未生效，问题同前                                                                                         |
| 3.10 代码块换行未生效           | `.cm-scroller` 是 **flex 容器**，`.cm-content flex-shrink: 0` 使长行按 max-content 撑宽内容盒，永远到不了换行宽度 → 对齐 CM6 官方 lineWrapping：`.cm-content` 补 `flex-shrink:1 + white-space:break-spaces` | 通过                                                                                               |
| 3.2 起始页                 | 快捷键提示已删；新增 `show_author` 设置项（设置→界面→"起始页显示署名"，默认显示）                                                                                                                             | 通过                                                                                               |
| 3.3 Ctrl+F 第二下弹浏览器检索    | 浮条聚焦时 PM 收不到 keydown → Ctrl+F 移到 window **capture 阶段**统一拦截；已打开时聚焦回输入框                                                                                                          | 基本通过，但搜索替换框的UI稍微优化一下。两行按钮没有对齐，第一行中间有空隙，第二行右侧有空隙。                                                 |
| 3.5/3.6 菜单文案            | "== 高亮" → "高亮"                                                                                                                                                                 | 通过                                                                                               |
| 3.5/3.6 callout 单一/不可回退 | `wrapCallout` → `toggleCallout(view, type)`：同类型再按**回退原样**、异类型改写标记、普通引用块补标记；右键新增 Callout 子菜单（Note/Tip/Important/Warning/Caution），hover 展开、贴右缘自动左翻                               | 基本通过。但回退识别有点问题，如果是一个非note的callout，再点按钮会先变成note，再回退。应该直接回退。另外，callout样式方面，每种的logo和颜色应该完全不同，目前待完善。 |
| 提示7 通知                  | 未触发属预期（需外部改文件场景）                                                                                                                                                               | 搁置                                                                                               |
| 提示8 2.1 悬案              | 按复测结论搁置（清单 §八）                                                                                                                                                                 | 搁置                                                                                               |

**验证**：`npm run build`（tsc）✅、`cargo check` ✅、序列化回归 8 项 ✅。
**复测提示**：① Ctrl+, 在中英文输入法下都能开设置；② 浮条开着连按 Ctrl+F 不再弹浏览器检索；③ 代码块语言按钮弹出完整列表、选 rust/python 等有高亮；④ 长行代码块开换行立即软换行；⑤ 属性栏两列点击高亮同宽；⑥ 右键 → Callout → 选 Warning 看 `> [!warning]` 序列化，原处再点同类型回退为普通段落；⑦ 设置→界面 取消"起始页显示署名"后开空窗口确认。

---

## 三轮（2026-07-31，3.3 替换 + 3.9 浮层主题化）

- **3.3 替换**：浮条第二行（替换输入框 + 替换/全部）。替换走正常 PM 事务（自动保存接管）；命中区间永不跨块（扫描以 textblock 为单位），纯文本替换安全；"全部"从后往前、单事务（一次撤销整体回退）；替换当前后原下标自然指向下一个命中；Enter 在替换框 = 替换当前；只读/无命中禁用按钮。*逻辑通过，UI问题*
- **3.9 浮层主题化**：核查发现颜色/字体已全部经 `--crepe-*` 变量桥收口，无需逐组件改色。落地：`--crepe-shadow-1/2` 加重为深色浮层投影（与 toast/检索条/右键菜单同语言）；斜杠菜单/工具栏/链接提示框/语言选择浮层统一补 `1px var(--ob-border)` 边框；斜杠菜单与语言浮层圆角统一 8px。
- **3.7 图源链路**：静态核查——CSP 与 assetProtocol 早已就绪，缺口只在 Crepe 不转换本地路径。用 `proxyDomURL` 官方钩子接通：网络图原样、本地绝对路径 `convertFileSrc`、相对路径相对当前 md 目录解析（含 `.`/`..` 归一化、`%20` 解码、盘符/UNC 保护）；只改 DOM src 不动文档原文；拖入换文件后自动跟随新目录。**样式观感实测待你**（三类图源 + 圆角/边框/失败占位/caption）。

**验证**：`npm run build` ✅、序列化回归 8 项 ✅。
**复测提示**：⑧ Ctrl+F 输入"替换为…"→ 点"替换"看当前命中被换并自动跳到下一个；点"全部"后 Ctrl+Z 应一次整体回退；⑨ 斜杠菜单/选中工具栏/悬浮链接看浮层边框、阴影、圆角是否与其他浮层协调（观感以你为准，不协调再调）；⑩ md 里放一张相对路径图（如同目录 `![](a.png)`）、一张绝对路径图、一张网络图，看三类是否都能渲染。*反馈：确实三类都能渲染，粘贴文件走的仍是内部写法，不知是否符合预期*

---

## 四轮（2026-07-31，批次 5 自动化测量 + 批次 6 发布检查）

- **批次 5 性能基线**：`scripts/gen-perf-fixture.mjs` 造三档夹具（259B / 10万字 / 923KB），PowerShell 自动化测量。结果：**冷启动标题就绪 1059ms（热启动 \~180ms）**；内存合计：空文档 472MB / 10万字 670MB / 1MB 级 1006MB（WebView2 增量口径，含共享运行时，有噪音）；1MB 打开到 CPU 安静 4.5s；5 窗口×10万字 591MB，无线性驻留。数字已填清单附录 A。**打字跟手/滚动帧率需你手感复测**——卡就启用装饰器增量预案。*反馈：手感很不错，强于OBsidian*
- **批次 6 发布检查**：README.md 初稿（截图占位待补）；`scripts/collect-licenses.mjs` 归置 221 份许可入 zip `licenses/`（含 AnuPpuccin GPL-3.0 原文 + NOTICE 署名）；exe 版本信息已嵌入；`.github/workflows/release.yml`（tag v\* → 构建 → zip → Release，未实跑过）。
- **⚠️ 留你决策**：根 LICENSE 是 MIT，但打包的 AnuPpuccin 衍生 CSS 是 GPL-3.0——许可文本已按 GPL 义务归置，根 LICENSE 改不改 GPL-3.0 由你定。**决定：改GPL-3.0**
- **msi 打包失败**：本机 WiX light.exe 环境问题（ASCII 对照实验排除中文因素）；发行物是绿色 zip，统一走 `--no-bundle`，不受影响。

**zip 已重封 4.54MB**（含全部修复 + licenses），release/Oblet-0.1.0-win-x64.zip。

---

## 五轮（2026-07-31，批次 4 设置体系全落地）

- **4.1 毛玻璃**：window-vibrancy 0.6 + 窗口 transparent + `set_window_effect` 命令（mica/acrylic/关）+ 设置→界面下拉（默认关）+ CSS 透明链路；跨窗口广播。默认关时视觉与之前一致。
- **4.4 快捷键系统**：`src/commands.ts` 命令注册表 + 统一捕获派发器（`e.code` 物理键码）；save/search/settings 三命令迁入；覆盖存 `settings.json editor.keymap`；设置→快捷键节点击改键（捕获模式）、双击恢复默认。
- **4.2 设置页**：随 4.1/4.4 收口为四节（排版/编辑器/界面/快捷键），视觉与其他浮层同语言。
- **4.5 当前块底色原型**：核查发现 body 上其实没有 anp-current-line 类（清单原假设有误）。原型已上：光标所在顶层块 7% mauve 淡底色，由 main.ts 挂的 `anp-current-line` 类门控——**留你观感评审：保留或移除（删挂类+插件+CSS），二选一**。

**验证**：`npm run build` ✅、`cargo check` ✅、序列化回归 8 项 ✅。
**复测提示**：⑪ 设置→界面→窗口效果选 Mica/Acrylic 看材质透出与可读性，关回后确认恢复原样（默认关 = 与之前一致）；⑫ 设置→快捷键 把"保存"改成 Ctrl+Alt+S 试生效，双击恢复默认；改后另开窗口确认同步；⑬ 光标在段落/列表/callout 间移动看淡底色跟随，评审去留。

*反馈：毛玻璃问题较大。两种方案的窗口透明均能生效，但文档主体并未生效，观感是两边透明中间紫黑。另外homepage的元素（如logo、设置按钮等）也没有针对透明做优化。Acrylic底色太亮，和我们的紫黑界面观感割裂。*

---

## 六轮（2026-07-31，三轮反馈修订 + LICENSE 决策落地）

| 反馈                        | 根因与修法                                                                                                                                                                                                | 反馈                                                                                                                                      |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------- |
| Ctrl+, 仍不生效               | 按批示默认改 **Ctrl+/**（注册表 defaultCombo + 按钮 title + README 快捷键表；覆盖机制不变，旧设置里有自定义的不受影响）                                                                                                                    | 通过。但设置界面的滚动条未能继承全局统一外观                                                                                                                  |
| 替换浮条两行不对齐                 | 两行共用同一 5 列网格（`display: contents`），替换输入框跨前 3 列、按钮对齐检索行 next/close 列，消除行间空隙                                                                                                                            | 仍然不满意，中间似乎空了三格（第一行一个第二行两个），留白删掉，                                                                                                        |
| 属性栏第一列不可见                 | 二轮删 field-sizing 后，`td.fm-key width:1%` + input 百分比宽度不贡献 intrinsic → 列被压回 size 属性宽度。修法：键名列固定 30%，JS 侧删掉 size 估算                                                                                      | 属性栏现有特性全部通过。但目前用户似乎无法在Oblet中对一篇没有属性栏的笔记创建属性。                                                                                            |
| 非 note callout 点按钮先变 note | `toggleCallout` 加 `revertAnyType`：工具栏按钮对**任意** callout 直接回退；右键菜单各项保留"同类型回退、异类型改写"                                                                                                                    | 通过                                                                                                                                      |
| callout 五种样式趋同            | 兜底配色拆分为 Ob 官方语义色（Catppuccin 调色）：note=蓝、tip=青、important=紫、warning=橙、caution=红；图标差异化：important 改消息框感叹、caution 改八角警告（warning 保留三角）                                                                      | 通过                                                                                                                                      |
| 代码块语言选择仍空白                | **jsdom 全链路冒烟（scripts/verify-languages.mjs）**：codeBlockConfig.languages=143 ✅、点击浮层弹出 ✅、列表渲染 143 项 ✅——配置链与交互均无问题，此前"未生效"应是测了修复前的旧包。请用本论 zip 复测                                                        | 用最新包测试。澄清一下：如果我想创建一个python代码块，我可以\`\`\`python回车，这样可以成功也有语法高亮。但生成的代码块语言无法再改。点击弹出search 搜索框，但仍没有选项，输入任意文本按回车也没有反应。可能是我们的魔改代码破坏了原版组间的某些行为。 |
| 根 LICENSE 决策              | 按批示改 **GPL-3.0**：根 LICENSE 与发行包 Oblet-LICENSE.txt 均替换为 GPL-3.0 原文，README 许可段同步说明（因 AnuPpuccin 衍生样式整体按 GPL 发布）                                                                                        | 通过                                                                                                                                      |
| 毛玻璃三问题                    | ①"中间紫黑"=`.milkdown` 的 `--crepe-color-background` 不透明 → ob-vibrancy 下透明化（代码块/表格自带色块保留，浮起且可读）；②设置按钮半透明化（原不透明块在材质上突兀）；③Acrylic tint 取 Mocha base (30,30,46)、alpha 60→150（60 时桌面亮内容大量透入显亮）               | 已看到修改成效。Mica方案整体要再透一些，背景色需要略浅一点点，不透明度调低。Acrylic方案仍然太透了（可能有显示问题，基本全透），背景应该是深灰色的，不透明度70%左右，且未对搜索栏浮窗和设置界面做适配。再迭代一轮，如果不行，考虑删掉Acrylic方案。     |
| ⑩ 粘贴图片"内部写法"              | **不符合预期，实为缺陷**：Crepe 默认 onUpload 是 `blob:` 内存 URL，保存进 md 重开即失效。新增 `save_image_asset` 命令：粘贴/拖入图片复制到 md 同目录 `assets/`（重名自动 -1/-2 去重），文档写 `![](assets/foo.png)` 相对引用——渲染走已有 proxyDomURL，与 Obsidian 双向兼容 | 通过                                                                                                                                      |

**验证**：`npm run build` ✅、`cargo check` ✅、序列化回归 8 项 ✅、jsdom 语言选择冒烟 ✅。
**复测提示**：⑭ Ctrl+/ 开设置（中英文输入法都试）；⑮ 属性栏第一列键名可见、两列高亮框同宽；⑯ 非 note callout（如 warning）上点工具栏引号按钮应**直接**回退为普通段落；⑰ 右键 → Callout 五种各建一个，颜色与图标应两两不同；⑱ 代码块语言按钮应弹出 143 种语言列表（若仍空白请记录是否连搜索框都无）；⑲ 窗口效果 Mica/Acrylic 下文档主体应透出材质、Acrylic 应呈深紫黑；⑳ 往文档里粘贴一张截图，md 旁应生成 `assets/` 目录，保存重开后图片仍在。

**仍待你**：4.5 当前块底色去留（复测提示⑬）——其余反馈项均已处理。

---

## 七轮（2026-07-31，六轮反馈修订 + 语言悬案结案）

| 反馈                | 根因与修法                                                                                                                                                                                                                                                                                                        | 新反馈                                                                                                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 替换浮条中间留白（红圈图）     | 根因：input 钉死 `width:180px`，1fr 列轨比它宽时右侧留白。改两行各自 flex、输入框 `flex:1` 填满，命中计数叠进搜索框内右端（VS Code 风格），行尾按钮自然右缘对齐                                                                                                                                                                                                      | 通过                                                                                                                                                                                     |
| 设置界面滚动条不统一        | 滚动条样式原挂在 `.markdown-rendered` 作用域，设置浮层在 body 下吃不到 → 提升为全局                                                                                                                                                                                                                                                    | 通过                                                                                                                                                                                     |
| 悬浮窗网址点击无响应        | webview 对 `target=_blank` 不做任何处理。新增 `open_url` 命令（rundll32 调默认浏览器，零新依赖，仅放行 http/https）+ 全局 click 委托：浮层里的链接调浏览器，正文编辑态链接不劫持                                                                                                                                                                                    | 通过                                                                                                                                                                                     |
| 无属性栏笔记无法创建属性      | 右键菜单新增"添加笔记属性"（无 frontmatter 时可用）：文首插入空属性节点并聚焦键名虚拟行；所有行删光时整个节点自动移除，不留空 `---` 围栏                                                                                                                                                                                                                              | 通过                                                                                                                                                                                     |
| **语言选择悬案结案**      | **四个环境实测全部正常**（jsdom 冒烟、esbuild+Edge、vite dist+Edge、真实 release exe 的 WebView2 CDP 直连）：143 种语言、浮层 410px、零报错。根因是**单实例劫持**：你机器上常驻着早期 `npm run tauri dev` 的 debug 实例，单实例锁按 AppID 不分 debug/release——每次双击 zip 新包，进程被转发到那个**陈旧 dev 窗口**（旧前端快照，停在 languages 空数组修复之前）就退出了。修法：单实例插件仅 release 注册（debug 不持锁），此后新包启动不再被劫持 | 并未修复。我认为还是渲染序列问题。我发现了一个非常神奇的现象：对于在打开时解析并渲染的已经存在的代码块，所有功能完全正常，搜索框有语言选项。然而对于用户在Oblet中（斜杠或手敲\`\`\`）新生成的代码块，因为创建时里面并没有任何代码，所以搜索框也没有弹出任何选项，这很奇怪。好在已经实现了空格自定义语言，即使没有这个候选框也可以正常输入语言类型，高亮也没问题。 |
| 搜索框回车无反应（1.4 心愿单） | 原版组件搜索框回车本就无行为（只能点选列表）。新增 `languageFreeInputPlugin`：搜索框输入任意语言名回车即设置——命中预设照常懒加载高亮，自定义语言无高亮纯文本、围栏保留原文（与 Ob 一致）。CDP 实测："mylang" 回车 → 按钮变 mylang、浮层收起 ✅                                                                                                                                                          | 通过。这个已经生效了                                                                                                                                                                             |

**排查副产品**：`scripts/repro-dist.mjs`（dist 产物 CDP 冒烟）、`scripts/repro-webview.mjs`（真机 WebView2 CDP 直连采集）入库，改 Crepe 配置后可作真机回归。

**验证**：`npm run build` ✅、`cargo check` ✅、序列化回归 8 项 ✅、jsdom 语言冒烟 ✅、CDP 自由输入 ✅。
**复测提示**：㉑ 替换浮条两行无中间留白、计数在搜索框内右端；㉒ 设置界面滚动条与编辑器一致；㉓ 悬停链接点网址应调默认浏览器；㉔ 无属性笔记右键 → 添加笔记属性 → 填键值保存看 `---` 围栏，删光行属性栏应消失；㉕ 代码块语言浮层应有 143 项（**测前关掉所有 dev/旧窗口**，新包已不被劫持），搜索框输 `mylang` 回车看自定义语言生效。
**注意**：你测试粘贴图片功能时在仓库根生成的 `assets/头像.jpg` 已删（测试产物）；运行中的 dev 旧实例我已在排查时结束。
**下轮待办**：毛玻璃二轮迭代（Mica 调透调浅、Acrylic 修"基本全透"+深灰 70% 不透明、搜索栏/设置浮层适配——不行则删 Acrylic）；exe/zip 随下轮一起重打。

---

## 八轮（2026-07-31，空代码块浮层悬案真结案 + 毛玻璃二轮）

| 反馈              | 根因与修法                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 新反馈                                                 |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------- |
| 空代码块语言浮层空白（真根因） | 你的"渲染序列"直觉方向正确，定性精化为**空 vs 非空**：新建代码块、删空内容的代码块都会空白，已存在且有内容的正常。真根因是**我们自己的 CSS**——`.milkdown-code-block { overflow: hidden }`（为圆角裁剪加的）把 NodeView dom 内的语言浮层裁掉：空代码块只有一两行高，ul 410px 向下溢出盒子被整体裁掉。**DOM 里 143 项一直在**（liCount 正常），只是视觉不可见；有内容的代码块够高，浮层落在盒内所以正常。此前四环境验证全用 getBoundingClientRect 量**布局尺寸**——量不出 overflow 裁剪，于是全报"正常"。本轮改 `elementFromPoint` 像素级取样实锤：取样点命中的是底层 `.markdown-rendered`，浮层像素根本没渲染出来。修法：`.milkdown-code-block:has(.language-button[data-expanded="true"]) { overflow: visible }`——浮层展开时放开裁剪（data-expanded 是组件原生状态属性），平时圆角裁剪不变。修复后取样点命中 `LI.language-list-item` 本身 ✅ | 终于修复，目前没有遇到其他问题。                                    |
| 毛玻璃二轮           | ① **Mica**：它是系统**不透明**材质，"再透"物理上给不了，改为罩一层 text 色 5% 薄纱把材质色调调浅（"背景色略浅"）；② **Acrylic**：tint alpha 150→**179**（≈70% 不透明，深灰目标值）——本轮仍"基本全透"的话，按你的预案下轮直接删 Acrylic 只留 Mica；③ **浮层适配**：搜索栏浮条/设置面板/toast/右键菜单统一半透明 `rgba(ctp-base, 0.72)` + `backdrop-filter: blur(24px)`（模糊背后正文，VS Code 风格），搜索框 input 0.45。body 新增 `ob-vibrancy-mica/acrylic` 子类区分两种材质的叠加策略                                                                                                                                                                                                                                   | 这轮修改效果不好。回滚Mica，取消薄纱材质。Acrylic alpha值→200.浮层适配不错，保留 |

**排查方法论更新**（repro-dist.mjs 已固化）：浮层类"看不见"问题，**布局尺寸 ≠ 视觉可见**，必须 `elementFromPoint` 像素级判定。本轮新增"空代码块浮层可见性"断言入库（mock payload 内含一个空代码块）。

**验证**：`npm run build` ✅、`cargo check` ✅、jsdom 语言冒烟 143 ✅、序列化回归 8 项 ✅、repro-dist CDP（已有块 143 + 空块可见性 + 自由输入）✅。
**复测提示**（新包）：㉖ 斜杠/手敲 \`\`\` 新建代码块 → 语言按钮应弹出 143 项；把已有代码块内容删空再点，也应有；㉗ Mica 下文档区色调应略浅于之前；㉘ Acrylic 应呈深灰、约七成不透明——**仍基本全透就告诉我，下轮删 Acrylic**；㉙ Mica/Acrylic 下 Ctrl+F 浮条与设置面板应半透明 + 背后文字模糊，不再是实心紫黑块。
**exe/zip**：本轮重打（见下）。



*批示：除上述外还遇到了新问题，之前做的底部留白不是一个固定值，而是会随着窗口大小变化，最大化后直接变成1.5屏的留白，这是没必要的，改为固定值。*

---

## 九轮（2026-07-31，八轮反馈修订 + 底部留白固定值）

| 反馈 | 根因与修法 |
| ---- | --------- |
| 空代码块浮层修复确认 | ✅ 通过，无其他问题 |
| 毛玻璃三轮 | 按批示：① **回滚 Mica 薄纱**（删 `ob-vibrancy-mica .milkdown` 5% text 色叠加，Mica 恢复系统材质原样透出；`ob-vibrancy-mica/acrylic` 子类随之移除）；② **Acrylic alpha 179→200**（78% 不透明）；③ 浮层半透明 + backdrop-filter **保留** |
| 底部留白随窗口膨胀 | `padding: 24px 32px 40vh`——vh 单位随窗高线性膨胀（720px 窗口 288px、最大化 576px 近半屏）。改固定 **280px**（≈ 默认窗口下原 40vh 观感，任何窗高一致） |
| 4.5 当前块淡底色 | ✅ 按批示**保留**（决策闭环，移出待办） |
| 3.7 图片样式 / 3.8-3.9 callout 与浮层观感 / 5.2 手感 | ✅ 全部通过（"操作丝滑"） |

**验证**：`npm run build` ✅、`cargo check` ✅。
**复测提示**（新包）：㉚ Mica 应恢复为系统材质原样（无薄纱偏色）；㉛ Acrylic 应更实（78% 不透明）——这轮还不行就按预案删 Acrylic；㉜ 窗口最大化拉到底，留白应与普通窗高一致（固定 280px，不再膨胀）。
**exe/zip**：本轮重打。

---

## 十轮（2026-08-01，Acrylic 深紫黑 + 快捷键扩容 + 设置大扩容）

| 反馈 | 根因与修法 | 新反馈 |
| ---- | --------- | ------ |
| Acrylic 继续调 | 按批示：tint 底色 (30,30,46) mocha base → **(24,18,42) 非常深的紫黑色**，alpha 200→**250**（98% 不透明，近实底）。alpha 演进 60→150→179→200→250 | |
| 底部留白"没有改善" | **实测定案**（`scripts/measure-bottom-space.mjs` 入库，真机 CDP + 视口覆盖双尺寸测量）：滚到底的空白在任何窗口尺寸下恒为 **287px**（280 留白 + 7px 末段下边距），不膨胀——九轮修复是生效的。用户看到的"随窗口变大"是另一回事：**短文档不滚动时，末行到窗口底边的视口内空白** = 视口高 − 内容高，窗口越大（或越宽、回流后行数越少）空白越大——这是所有编辑器（Obsidian/Typora 同）的物理现象，不是 bug。本轮把留白值做成设置项（界面 → 底部留白 px，0–800 可调），请用它实测对比 | |
| 当前块底色过醒目 | mauve alpha 0.07 → **0.045**；另设强度档（编辑器 → 块底色强度：几乎不可见 0.02 / 淡 0.03 / 适中 0.06 / 醒目 0.09 / 很醒目 0.13） | |
| 快捷键扩容 | 经命令注册表接入（默认组合均可在设置 → 快捷键改键）：**Alt+1~6** 一至六级标题（toggle 语义：已是该级再按回正文）、**Ctrl+B** 加粗、**Ctrl+I** 斜体、**Ctrl+H** 高亮、**Ctrl+`** 行内代码。window 捕获派发 + preventDefault，与 Milkdown 内置 Mod-b/i 不重复触发；只读文件静默放行；焦点在输入框/设置面板时快捷键不穿透到后台文档（新防线） | |
| 设置大扩容 | 新增 10 项（"之后按需删减"）：**排版**——标题字体、行高（1.2–2.4）、段间距（0–2em）、标题缩放（0.7–1.5）、正文颜色、强调色（颜色 + ✕ 复位）；**编辑器**——自动保存开关、自动保存延迟（200–10000ms，settings.rs 里躺了七轮的 auto_save_delay_ms 终于接线）、块底色开关、块底色强度；**界面**——底部留白（0–800px）。全部跨窗口广播即时生效 | |

**架构注记**：设置面板输入类型归纳为四种通用通道——`data-typo`（文本）/`data-num`（数字，按 min/max 夹取）/`data-color`（颜色 + 复位钮）/`data-check`+`data-select`（布尔/枚举），空值一律写回 null（跟随默认，settings.json 自说明）。排版应用侧新增 `--ob-line-height/--ob-para-gap/--ob-heading-scale/--ob-bottom-padding/--ob-active-alpha/--h1-font/--text-normal/--interactive-accent` 内联变量（body+#app 双写，压主题类规则）。新增 `__oblet` 自动化验证钩子（selectLastParagraph/getMarkdown）——合成键盘事件驱动不了 DOM 选区（浏览器忽略非可信按键），选区建立与序列化断言必须走编辑器内部 API。

**验证**：`npm run build` ✅、`cargo check` ✅、序列化回归 8 项 ✅、jsdom 语言冒烟 143 ✅、repro-dist CDP（浮层可见性 + **快捷键全链路序列化断言**：`**正文段落。**`/`==…==`/`` `…` ``/`## …` toggle 往返全部字节级干净）✅。
**复测提示**（新包）：㉝ Acrylic 应为深紫黑近实底（250）；㉞ 当前块底色应明显收敛，不够再在设置里调强度档；㉟ Alt+1~6 变标题（再按同级回正文）、选词后 Ctrl+B/I/H/`；㊱ 设置面板四节逐项试——行高/段间距/标题缩放/留白/颜色改完应全窗口即时生效，**重点用底部留白滑值对比你的"膨胀"场景**（若 0 与 280 无差别，说明看到的就是视口内空白，属物理现象）；㊲ 自动保存关掉后应只剩 Ctrl+S 保存。
**exe/zip**：本轮重打。

---

## 十一轮（2026-08-01，Acrylic 终审删除 + 设置删减定稿 + 选区拖动定性）

| 反馈 | 根因与修法 | 新反馈 |
| ---- | --------- | ------ |
| Acrylic 终审 | **方案删除**（设置下拉、commands.rs `set_window_effect` acrylic 分支、相关 CSS 全撤）。留档一句于打磨清单 4.1：`apply_acrylic(tint (24,18,42,250))` 深紫黑亚克力，后续适配浅色主题时或可复用。窗口效果设置项收敛为 **Mica 开关**（checkbox，window_effect = "mica"/null） | |
| 底部留白定稿 | 用户实测对比后定论 **0 为最佳观感**——`padding: 24px 32px 0`，`bottom_padding` 设置项、`--ob-bottom-padding` 变量、`measure-bottom-space.mjs` 全删。演进归档：40vh（随窗膨胀）→ 280px 固定 → 设置化 0–800 → **0 定稿** | |
| 当前块底色定稿 | 0.045 确认"默认刚好"，强度档设置项删（写死 0.045），显隐开关保留 | |
| 快捷键补充 | 全部所需快捷键实测通过；右键菜单删"高亮"项（Ctrl+H + 工具栏按钮已覆盖）；新增 **Alt+A** callout 包裹（与工具栏按钮同语义：note + revertAnyType） | |
| 选区无法拖动 | **是 bug 但本轮定性不修**：根因——Tauri 文件拖放（wry）在窗口上 RevokeDragDrop + 注册自家 IDropTarget，Chromium 的 OLE 拖动会话（页面内拖文字同走 OLE）被接管，非文件载荷一律 DROPEFFECT_NONE → 只剩禁止光标。曾实现鼠标事件自驱动版（插件 dragMovePlugin，dist CDP 实测移动成功），但**与文件拖入互斥**：禁 dragstart 后外部文件拖入被原生 OLE 会话吞掉，到不了 Tauri 的 IDropTarget。运行时无动态开关（tauri 2.11 无 set_drag_drop_enabled）。**取舍：保留文件拖入（核心交互），拖动移动回滚**（插件留空壳 + 注释留档，待 Tauri 运行时开关） | |
| 设置删减定稿 | 排版撤十轮全部新增项（恢复原 4 项：正文/等宽/界面字体 + 基础字号）并删"留空则跟随主题…"提示语；编辑器自动保存延迟写死 **500ms**（原 1000）、块底色强度写死删项；界面撤底部留白、窗口效果改 Mica 开关。settings.rs 同步清 10 字段（auto_save_delay_ms/title_font/line_height/paragraph_gap/heading_scale/active_block_alpha/bottom_padding/text_color/accent_color + default_delay 函数）；旧 settings.json 多余字段 serde 读时忽略、存时丢弃 | |

**验证**：`npm run build` ✅、`cargo check` ✅、序列化回归 8 项 ✅、jsdom 语言冒烟 143 ✅、repro-dist CDP（浮层可见性 + 快捷键全链路）✅、repro-webview 真机（设置面板收敛形态：4 文本 + 4 复选 + Mica 开关 + 无十轮残留控件；快捷键含 Alt+A callout 序列化断言 `> [!note] …` toggle 往返）✅。
**复测提示**（新包）：㊳ 设置 → 界面应只有 Mica 开关（无 Acrylic）；㊴ 拖 .md 进窗口换文件应正常（拖动移动已回滚，此项保的是它）；㊵ 选词 Alt+A 应包成 `[!note]` callout，再按回退；㊶ 自动保存延迟 500ms（打字后停顿即保存）；㊷ 右键菜单应无"高亮"项。
**exe/zip**：本轮重打。

---

## 十二轮（2026-08-01，标题键位写死 + 选区拖动真落地）

| 反馈 | 根因与修法 | 新反馈 |
| ---- | --------- | ------ |
| 标题快捷键出列表 | `Command` 接口加 `remappable` 标志（默认 true）——heading-1~6 设 false：设置列表不显示、keymap 覆盖被忽略（等于写死）。架构上比"UI 过滤"干净：覆盖不可能生效 | |
| 选区拖动（翻案落地） | **上轮定性有误，本轮翻案**。重读 wry 0.55 `drag_drop.rs` 源码：其 IDropTarget 每个回调开头都是 `enter_is_valid = hdrop.is_some()`，**非文件载荷（页面内拖文本无 CF_HDROP）第一时间 `return Ok(())` 放行**——OLE 层只有一个 drop target，但它是"文件才接管、文本全放行"的。上轮"禁 dragstart 后文件拖入失效"的担忧不成立（dragstart 是页面内事件，拦不到 OLE 层）。禁止光标的真因是 **HTML5 DnD 语义**：dragover 元素未 preventDefault 声明接受，浏览器就显示禁止符号；ProseMirror 默认无拖放 handler。修法只一层：dragstart 自描述标记（`application/x-oblet-drag`）+ effectAllowed move → dragover preventDefault（声明接受，禁止符号变可放置）→ drop 按落点 delete + mapping 修正 + replaceRange 移动选区。**文件拖入与文本拖动不同层，天然共存无需分区**。验证坑：合成 DragEvent 的 `dataTransfer` 必须是真 `new DataTransfer()`（stub 会被构造器拒绝）；CDP 断言 `# 标题正文段落。` 落点精确 | |

**验证**：`npm run build` ✅、序列化回归 8 项 ✅、repro-dist CDP（快捷键全链路 + **DnD 事件序列拖动断言**）✅、repro-webview 真机（拖动 `# 标题正文段落。` 落点精确 + 快捷键含 Alt+A + 设置面板 8 键位行）✅。
**排错纪实**（exe 嵌入资源陈旧连环坑）：真机验证一度 dragstart 不触发——排查链：① `cargo build --release` 不重嵌前端资源（dist 变了 exe 没变，页面仍"localhost 拒绝连接"）；② `npm run tauri build` 在脏 target 上 tauri-build 也不重跑 codegen（cargo clean -p oblet 清掉 3.6G 后 touch build.rs 才重嵌）；③ 嵌入后拖动用例仍失败，隔离场景实测成功——**是脚本内干扰**：dragMove 段放在 language-button 点击序列后，组件焦点/选区状态污染后续 DnD（教训：repro 脚本各段要么无状态、要么用 `__oblet.reset()` 重置文档，dragMove 段已提至最前 + shortcut 段加 reset）。
**复测提示**（新包）：㊸ **选中一段文字直接拖到别处**——应可移动（禁止符号变可放置光标）；㊹ **拖 .md 进窗口换文件应仍正常**（两功能共存是本方案的核心，务必同测）；㊺ 设置 → 快捷键列表应无 1~6 级标题 6 行（写死 Alt+1~6 仍生效）。
**exe/zip**：本轮重打。

---

## 深度清理（2026-08-01，非批示轮）

| 项 | 处置 |
| -- | ---- |
| **绿色包夹带本机个人设置**（最重要发现） | `release/Oblet/data/settings.json` 把私人字体（霞鹜臻楷 GB 等）、键位覆盖（Alt+D/Alt+S）、`auto_save: false`（与十一轮"默认开启"批示相悖）打进了 zip——新用户首启即继承开发者偏好。删 `release/Oblet/data`（运行时自建），重打 zip；`audit-zip.mjs` 新增"个人设置泄漏"断言防复发。CI 工作流本就不带 data/，本地打包口径已对齐 |
| `scripts/.repro/`（75MB） | 整删——其中 75M 全是 Edge headless 的 `.edge-profile` 缓存（repro-dist 每次重建）；`measure-long/short.md` 是已删的 measure-bottom-space.mjs（十轮）遗留夹具；`cdp-test.md`/`readme-shot.md` 由各脚本每次自写 |
| `release/Oblet_logo.png` | 删——图标制作期遗留，全仓无引用 |
| `plugins.ts` dragover 未用参数 | `view` → `_view`（`tsc --noUnusedLocals` 全仓仅此一处；cargo check 零警告） |
| 保留项排查结论 | 脚本全留：audit-zip/collect-licenses/verify-languages/repro-dist/repro-webview 是验证管线，gen-perf-fixture 性能夹具，capture-readme-shot README 截图，brighten/prepare-icon 图标管线（配合 CLAUDE.md 图标更换坑）；`ref/` 6.3M 参考素材按 CLAUDE.md 定位保留；`clear_acrylic` 调用保留（清旧版本残留，有意为之） |

**验证**：`npm run build` ✅、zip 重封 4.56MB、审计 MD5 MATCH + 222 许可 + bat 齐全 + 无泄漏 ✅。

---

## 测试提示（按改动风险排序，重点在前）

1. **高亮复合渲染（1.6，改动最大）**：==\*\*粗\*\*==（粗体红应透出）、==`粗体`==（代码应被包进高亮）、`` `==` ``（代码内 == 应保持字面不高亮）。顺手 Ctrl+S 后看源文件无变化。 ***实测基本没有问题***
2. **检索（3.3）**：Ctrl+F 开浮条 → 输入看命中计数与全文档高亮；Enter/Shift+Enter 循环跳转且滚动居中；**浮条开着时在正文按 Enter 应照常换行**；Esc 关闭；选中一段文字再 Ctrl+F 应预填。
3. **工具栏/右键（3.5/3.6）**：选中文字 → bubble 工具栏尾部出现高亮笔与引号两按钮，点了看 `==` 与 `> [!note]` 序列化是否正确；右键菜单各条目（粘贴若失败会提示用 Ctrl+V，属预期降级）。
4. **起始页（1.3/3.2）**：空窗口应见 logo+署名；**Ctrl+, 设置能打开**（1.3 的修复点）；改字号起始页立即生效。
5. **设置（3.10）**：设置 → 编辑器 → 代码块自动换行，长行代码块切换看效果；重开窗口应保持。*未生效*
6. **属性栏（1.2）**：单元格里应能看到闪烁光标。
7. **通知（3.4）**：外部改文件（有未保存内容时）弹 warn toast；可顺手验证断网/占用导致保存失败弹 error toast（不好模拟可跳过）。*跳了*
8. **2.1 悬案复测**：用 release zip 版反复拖拽多个不同 md 进同一窗口，看是否还有卡死；没有就按 dev-only 结案。*小概率触发，具体原因不明，暂时不管了*





