# Oblet 打磨清单（P1 阶段工作项）

> 定位回顾：轻量、快速、基于 Obsidian 和 AnuPpuccin 深度定制的独立 Markdown 编辑器。
> 本清单只做三件事——**修复已知问题、打磨操作体验、保障性能**，不引入新功能方向、不增加架构重量。
> 每条标注：优先级（P0 影响日常使用 / P1 明显可感 / P2 锦上添花）、工作量（小=半天内 / 中=一两天 / 大=需专题）。
> v1.1（2026-07-31）：按使用者批示重整——已验证项归档（§七）、否决项记录决策（§八），自提改进意见并入对应类别；视觉打磨不再以"与 Ob 完全一致"为目标（Oblet 已比 Ob 漂亮），以自我观感协调为准。

---

## 一、P0 关键项

- [ ] **检索与替换**（P0 / 中）
  现状：无。这是"编辑器"身份的硬缺口，PM 无内置方案。
  改进：自绘浮动搜索条（贴合设置浮层的视觉语言），纯装饰器高亮命中（不动文档），Enter/Shift+Enter 跳转、Esc 关闭；在搜索稳定后加基础替换（逐个替换 + 全部替换，替换走正常事务序列化无虞）。
  验收：长文档（>5000 行）搜索无明显卡顿；命中数显示；跳转后滚动居中。

- [ ] **高亮复合渲染 bug**（P0 / 中）
  现象：`==**粗**==` 等复合场景下，高亮内的加粗/斜体颜色不能正常加载（彩虹色定制的 anp-bold-red/anp-italic-green 在高亮内失效）；高亮与行内代码不兼容。
  排查方向：装饰器 run 的拼接逻辑（plugins.ts 对 mark 的分段处理）+ CSS 层叠（.ob-highlight 与 strong/em 颜色变量的优先级）。
  验收：`==**粗**==`、`**==粗==**`、`==*斜*==`、`==`代码`==` 各组合渲染正确，序列化无变化。

- [ ] **dev 模式拖拽新 md 卡死悬案**（P0 / 待定）
  线索（使用者实测）：dev 模式更改代码热修复后，拖拽新 md 渲染时**有概率直接卡死**，已发生数起。
  行动：先判断是否为 dev 环境特有（HMR 状态残留 + replaceAll/监听器换绑时序）——release 版同路径反复拖拽复测；若 release 不发生则降级为 dev-only 记录，若发生则收集卡死时的主进程/webview 状态。无稳定复现前不动手改序列化层。

- [ ] **渲染逻辑系统性梳理（"屎山"治理）**（P0 / 大）
  背景：Milkdown 底座经多轮魔改（保真层、装饰器、NodeView、各种兜底），需要从头到尾系统梳理：无用甚至副作用的代码能删就删，保持架构清晰，不能全靠一堆兜底机制撑着。
  方法：借助专门的 code review skill 做全面审查 + 人工定夺；每项"兜底机制"要么写出它防的具体问题（注释固化），要么删除。
  出口：frontmatter.ts / plugins.ts / setup.ts 每个 hack 都有据可查；回归测试全过。

## 二、交互打磨

- [ ] **快捷操作栏增强**（P1 / 中）
  把 callout 创建/编辑、==高亮== 开关加入 Crepe 工具栏（bubble toolbar，Milkdown 特性，使用者评价"还挺方便"）。
  验收：选中文本 → 工具栏出现高亮切换与 callout 包裹按钮；操作后序列化分别为 `==文本==`、`> [!note] 文本`，无侵入性改动。

- [ ] **右键菜单**（P1 / 中）
  现状：浏览器风格的右键菜单，没什么用。
  改进：自绘编辑器语境右键菜单（复制/粘贴/全选 + 高亮/callout 等格式操作，与工具栏共用命令），样式走通知系统同一套视觉语言。

- [ ] **底部留白**（P1 / 小）✅批示通过
  编辑容器 `padding-bottom: 40vh`（Ob 同款），末行可滚动到视口中部；不产生额外可序列化内容。

- [ ] **代码块自动换行开关**（P2 / 小）
  设置项：代码块软换行开/关（审美因人而异）。AnuPpuccin 已有 `anp-codeblock-edit-nowrap` 类（当前默认不换行），做设置开关 + CM6 配置联动。
  顺带检查：语言选择器下拉样式、复制按钮 hover、块内滚动条与主滚动条一致性。

- [ ] **Home/End 智能行为**（P2 / 小）
  Home 第一次到文本起始（跳过列表标记/引用缩进），第二次到物理行首。优先级自然降低（使用者的设备没有这两个按键），有空再做。

## 三、视觉与界面

> 方法更新：不再逐项对照 Ob——以 Oblet 自身观感协调为准，发现哪里"不对劲"就修哪里。

- [ ] **起始页设计**（P1 / 小）
  现状：无文件打开时是纯文本提示。
  改进：认真设计——居中排版 + 项目 logo（icons/icon-source.png）+ 作者署名 **弋鹓 | lnabc03** + 操作提示（拖入文件 / Ctrl+,）。样式走主题变量。这是新用户的第一个画面。

- [ ] **统一通知系统**（P1 / 中）
  现状：只读横幅、外部变更提示、保存失败提示是临时样式，语言不统一。
  改进：建立统一的 toast/通知体系（位置、圆角、底色、滑入动画、信息/警告/错误三级），所有现有提示迁移过去，后续新提示一律走该系统。

- [ ] **图片块样式实测**（P1 / 中）
  已知好消息：网络 URL 图片预览默认可用（CSP 已放行 https）。
  检查：圆角、边框/阴影、加载中/失败占位、块内对齐、caption 输入框字体；前端样式与后端 asset 协议都可能要调。本地绝对路径 + 网络 URL 两类图源都要测。

- [ ] **Crepe 浮层组件主题化**（P2 / 中）
  斜杠菜单、工具栏、链接提示框的圆角/阴影/hover/字体细节向 AnuPpuccin 浮层语言看齐（圆角 6~8px、hover 用 `--background-modifier-hover`）。在右键菜单/搜索条落地后统一过一遍，避免重复劳动。

- [ ] **callout 颜色与样式丰富**（P2 / 中）
  现状：装饰器方案已支持常见类型的图标与底色，覆盖类型有限。
  改进：补全 Ob 全部 callout 类型的图标映射；确认 AnuPpuccin 的 callout 定制（`anp-callout-sleek` 等）在装饰器 DOM 上的命中情况，不命中的样式手工桥接。

- [ ] **当前行高亮决策**（P2 / 大，慎重）
  body 上挂着 `anp-current-line` 类但 PM 无"行"概念，该定制永不生效。
  决策点：做"光标所在块淡底色"原型 → 自我观感评审 → 保留或移除 body 上的类，二选一，不留中间态。

## 四、设置体系（批示：最后一起做）

- [ ] **完善设置页面**（P1 / 大，压轴）
  汇总所有已落地的设置项统一设计：排版覆盖（现有）、代码块换行、回车习惯（如做）、快捷键（见下）……信息架构分组，视觉与通知系统/右键菜单同语言。
- [ ] **settings 默认值实体化**（P2 / 小）
  settings.json 保存一套完整默认值，而非全跟主题（空文件也写入默认 editor 节）。
- [ ] **快捷键系统（可设置）**（P2 / 大）
  内置键位映射层：命令注册表 + settings.json 键位覆盖 + 设置页键位编辑 UI。不依赖 vault/hotkeys.json（设计文档已定）。压轴项，随设置页面一起做。

## 五、性能保障（要有数字，不能靠感觉）

- [ ] **建立性能基线**（P0 / 小）
  冷启动到可编辑耗时、release 内存（空文档 / 10 万字文档）、安装包体积，填入附录 A。以后每次大改后复测对比。
- [ ] **大文档实测**（P0 / 中）
  造 1MB 级真实感 md，实测打开耗时、打字跟手度、滚动帧率、自动保存耗时。
  瓶颈预案（按需启用，不提前优化）：装饰器插件改增量计算（目前全文档重建 DecorationSet，打字卡顿最大嫌疑）；`getMarkdown()` 全量序列化耗时测量。
- [ ] **多窗口内存审计**（P2 / 小）
  5 窗口各载中等文档，总内存是否线性增长、有无异常驻留。

## 六、发布前最后一遍（P2 出口检查，备忘）

- [ ] README.md：简介、截图（深色主题 + 属性栏 + callout）、下载使用、register-md.bat 说明、AnuPpuccin attribution + Buy Me a Coffee 链接（GPL-3.0 义务）、SmartScreen 说明。
- [ ] LICENSE 与第三方许可归置：AnuPpuccin GPL-3.0、KaTeX/Milkdown 等依赖许可打包进 zip（`licenses/` 目录）。
- [ ] 版本号与关于信息：exe 文件属性经 tauri.conf.json 补齐；窗口标题格式定稿。
- [ ] GitHub Actions：tag 触发 Windows 构建 + 自动上传 zip 到 Release。

---

## 七、已验证无需改动（批示归档）

| 项 | 批示 |
|---|---|
| 列表编辑手感（Enter/Tab/嵌套/任务项） | 全部验证通过，默认已支持，不用改 |
| 表格编辑手感 | Crepe 默认做得非常完善，不用改 |
| 选区颜色 / 虚拟光标 / 输入法候选框 | 目测都没问题 |
| watcher 外部删除/重建边缘情况 | 无意测试过，好像可以 |

## 八、已决策不做 / 搁置

| 项 | 决策 | 理由 |
|---|---|---|
| 回车行为双模式开关 | 不做 | 两种行为在 Ob 中没有可视区别 |
| 可读行宽限宽 + 居中 | 不做 | 保持现状 |
| 打开文件恢复滚动位置 | 搁置 | 这块 bug 较多，暂缓 |

---

## 附录 A：性能基线（待填）

| 指标 | 数值 | 测量日期 | 备注 |
|---|---|---|---|
| 冷启动到可编辑 | | | |
| 内存（空文档） | | | |
| 内存（10 万字） | | | |
| zip 体积 | 4.4MB | 2026-07-31 | 首个绿色包 |
| exe 体积 | 10.8MB | 2026-07-31 | 含新图标 |
